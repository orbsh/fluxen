[package]
name = "fluxen"
version = "0.1.0"
edition = "2024"
description = "Facade crate: the Leptos rendering backend"

[dependencies]
ui_leptos = { path = "../ui_leptos", optional = true }

[features]
leptos = ["dep:ui_leptos"]
